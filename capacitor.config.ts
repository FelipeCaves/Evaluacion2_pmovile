import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'felipe_cuevas_009v',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
